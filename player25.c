#include<stdio.h>
#include<string.h>
int main()
{
	int i,j,temp,a[i],n;
	printf("enter the elements");
	scanf("%d",&n);
	printf("enter the array elements");
	scanf("%d",&a[i]);
	for(i=0;i<n;i++)
	{
		for(j=0;j<n;j++)
		{
			if(a[i]>a[j])
			{
			temp=a[i];
			a[i]==a[j];
			a[j]=temp;
		}
		printf("ascending number are");
		for(i=0;i<n;i++)
		{
			scanf("%d",&a);
		}
}
}
	return 0;
}
